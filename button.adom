tag Button [
  if (props.link != '#') [
    a href={props.link} [
      button | {props.text} |
    ]
  ] else [
    a href='/' [
      button | {props.text} |
    ]
    span class={[
      'class1',
      'class2'
    ]} | hi |
  ]
]

module test -->
  function foo () {
    console.log('bar')
  }
<--

export Button
