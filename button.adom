tag Button [
  if (props.link != '#') [
    a href={props.link} [
      button | {props.text} |
    ]
  ] else [
    a href='/' [
      button | {props.text} |
    ]
  ]
]

module test -->
  function foo () {
    console.log('bar')
  }
<--

export Button
